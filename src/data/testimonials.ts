import { Testimonial } from '../types/testimonial';

/**
 * Testimonials Data Source
 *
 * ZERO-FABRICATION POLICY:
 * This array is intentionally empty by default.
 * Testimonials will be populated ONLY when verified client feedback is supplied by P.T. Selvam.
 * The UI components are built to handle empty states gracefully without visual breakage.
 */
export const TESTIMONIALS_DATA: Testimonial[] = [];
